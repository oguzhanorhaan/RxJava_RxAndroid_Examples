<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.operators">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Activites.FilterOperatorActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activites.MergeOperatorActivity">
        </activity>
        <activity android:name=".Activites.ConcatOperatorActivity"></activity>
        <activity android:name=".Activites.SwitchMapOperatorActivity" />
        <activity android:name=".Activites.ConcatMapActivity" />
        <activity android:name=".Activites.FlatMapActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>